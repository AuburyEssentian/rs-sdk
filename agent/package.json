{
  "name": "bot-agent",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "rsbot": "./cli.ts"
  },
  "scripts": {
    "agent": "bun run agent-service.ts",
    "agent:dev": "bun run --watch agent-service.ts",
    "gateway": "bun run gateway.ts",
    "gateway:dev": "bun run --watch gateway.ts",
    "cli": "bun run cli.ts",
    "login": "bun run login.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.14",
    "@anthropic-ai/sdk": "^0.39.0",
    "puppeteer": "^24.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
